import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './07_event_bus/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);

