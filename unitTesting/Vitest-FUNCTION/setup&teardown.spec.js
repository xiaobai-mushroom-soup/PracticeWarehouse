import { test, beforeEach, beforeAll, afterEach, afterAll } from 'vitest'

beforeEach(() => {
    return () => {
        // afterEach
    }
})
beforeAll(() => {
    return () => {
        //afterAll
    }
})
test(() => { })
afterEach(() => { })
afterAll(() => { })